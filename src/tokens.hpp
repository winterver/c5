#pragma once

// tokens
enum {
	// primitive types
	VOID = 256, CHAR, SHORT, INT, LONG, FLOAT, DOUBLE, STRUCT,
	// identifer & literals
	IDENTIFIER, NUMBER, STRING, DECIMAL,
	// multi-character operators
	//
};
